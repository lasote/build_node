os: windows
language: bash
before_install:
  - choco install python --version 3.7.2
  - python --version
  - C:/Python37/python.exe --version
  - C:/Python37/Scripts/pip.exe --version
install:
  - run.bat
